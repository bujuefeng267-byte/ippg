# rPPG 仓库：同学协作上传教程

更新时间：2026-09-04

仓库地址：<https://github.com/bujuefeng267-byte/rppg>

本文负责讲解“如何把同学的代码和文件贡献进仓库”。环境部署、算法运行和实验分析另见 [PROJECT_OPERATIONS.md](PROJECT_OPERATIONS.md)。

## 1. 先理解：公开不代表任何人都能直接上传

任何人可以查看和下载公开仓库，但只有获得写入权限的人才能推送分支。推荐同学加入为协作者，然后采用“个人分支 → Pull Request（合并申请）→ 审核 → main”的流程。[GitHub 权限说明](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/repository-access-and-collaboration/permission-levels-for-a-personal-account-repository)

没有协作者权限也能通过 Fork 提交贡献，见第 9 节。

> 本教程不表示已经邀请任何同学，也不表示仓库已经启用了分支保护。以下邀请和审核规则需要仓库主人实际执行。

## 2. 仓库主人先做：邀请同学

由 `bujuefeng267-byte` 登录 GitHub：

1. 打开仓库，点击 **Settings**。
2. 在侧栏 **Access → Collaborators** 中点击 **Add people**。
3. 输入同学自己的 GitHub 用户名，确认是正确账号后发送邀请。
4. 同学在邮件或 GitHub 通知中接受邀请；仅发送邀请而未接受，还不能正常协作。

界面名称可能随 GitHub 更新略有变化，参照 [GitHub 官方邀请步骤](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/repository-access-and-collaboration/inviting-collaborators-to-a-personal-repository)。

不要共用仓库主人的密码、验证码、令牌或 SSH 私钥。每位同学使用自己的 GitHub 账号和授权。

## 3. 同学首次准备

### 3.1 安装工具并选择终端

先安装 [Git](https://git-scm.com/downloads)。可在 Windows PowerShell、Git Bash 或 WSL 中运行下面的 Git 命令；一次操作始终使用同一个终端环境。

```bash
git --version
```

建议需要运行本项目算法的同学使用 WSL/Linux。Windows 浏览器登录成功不代表 WSL 的 Git 已经获得推送凭据。

### 3.2 下载仓库

在你自己选定的项目父目录中运行：

```bash
git clone https://github.com/bujuefeng267-byte/rppg.git
cd rppg
git remote -v
```

若已经下载过该仓库，直接进入现有目录，不必再 `clone`，也不要重新 `git init`。不要照搬仓库主人的 `/home/fengbujue/项目/rppg识别` 路径；那只是主人电脑上的位置。

### 3.3 设置自己的提交身份

将示例文字替换成你自己的信息，命令只影响当前仓库：

```bash
git config user.name "你的姓名或GitHub用户名"
git config user.email "你的GitHub已验证邮箱或noreply邮箱"
```

公开提交的邮箱会进入 Git 历史。希望隐藏真实邮箱时，从自己的 GitHub **Settings → Emails** 复制 noreply 地址，不要猜地址，也不要使用仓库主人的邮箱。

### 3.4 登录推送账号

如果安装了 [GitHub CLI](https://cli.github.com/)，在当前环境执行：

```bash
gh auth login
gh auth setup-git
gh auth status
```

登录时选择 GitHub.com、HTTPS、网页授权，使用本次产生的验证码及时完成授权。也可以使用已配置好的 Git Credential Manager 或 SSH。

浏览器密码不是 Git HTTPS 推送密码。不要将令牌写入远程 URL、源码、文档、截图或聊天消息。过期设备码需要重新申请，不能复用他人的授权码。

## 4. 推荐流程：第一次贡献代码

下面以同学 `xiaoming` 新增运动分析代码为例。分支名只是示例，换成“自己的名字/具体任务”，每个任务一个分支。

### 4.1 更新 main，再创建个人分支

先检查工作区；有未提交修改时，先保存并整理，不要直接切换分支。

```bash
git status
git switch main
git pull --ff-only origin main
git switch -c xiaoming/motion-analysis
```

不要让所有同学直接在 `main` 上提交。

### 4.2 放入或修改文件

代码放入仓库内合适位置。运行输出使用自己的独立目录，例如 `results/xiaoming/trial_001/`，避免覆盖现有 `results/vid/` 和其他同学的结果。

若新增实验，附上简短说明：数据来源、使用许可、算法、参数、运行命令、是否有同步真值，以及已知限制。

### 4.3 检查并提交

假设修改的是 `scan_motion.py`：

```bash
git status
git diff -- scan_motion.py
git add -- scan_motion.py
git diff --cached --stat
git diff --cached
git diff --cached --check
git commit -m "Improve motion scan analysis"
git push -u origin xiaoming/motion-analysis
```

`git add` 后的路径要改成你实际需要上传的文件，可列出多个路径。初学时优先逐个指定文件，不要未经检查就用 `git add .`。

注意：`git commit` 只保存到本机；`git push` 才上传到 GitHub。`git diff` 不显示未跟踪文件的正文，要结合 `git status` 并打开新文件检查。

### 4.4 提交 Pull Request

1. 打开仓库，点击 **Compare & pull request**，或 **Pull requests → New pull request**。
2. 确认目标为 `base: main`，来源为 `compare: xiaoming/motion-analysis`。
3. 写清“做了什么、为什么修改、如何测试、是否新增数据”。
4. 点击 **Create pull request**，请仓库主人或其他同学审核。
5. 根据审核意见在同一分支修改、提交并再次 `git push`，原 PR 会随之更新。
6. 审核通过后由有权限的人合并，不必为每次修改重新创建 PR。

参照 [GitHub 创建 PR 教程](https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/proposing-changes-to-your-work-with-pull-requests/creating-a-pull-request)。

可复制到 PR 描述中的模板：

```text
修改内容：
修改原因：
运行/测试命令：
测试结果：
是否新增数据：
数据来源与公开授权：
是否影响已有静态/动态实验结果：
```

## 5. 后续继续开发和同步

同一任务尚未合并时，在原分支继续修改：

```bash
git status
git add -- 你实际修改的文件路径
git commit -m "说明本次修改"
git push
```

任务已合并、准备开始新任务时，确认工作区干净，再运行：

```bash
git switch main
git pull --ff-only origin main
git switch -c xiaoming/next-task
```

`--ff-only` 失败时说明本地与远程存在分叉，应先查清原因；不要用强制推送解决。

## 6. 上传前检查：哪些文件可以放进去

| 文件类型 | 建议 |
|---|---|
| Python、Shell 源码和 Markdown 文档 | 检查后上传 |
| requirements.txt | 新增依赖时同步更新 |
| 合成测试数据 | 确认不含真人或秘密数据后上传 |
| 真人心率、波形 CSV、JSON 和图表 | 先核对数据许可、隐私及公开授权，再提交 |
| 真人原视频、第三方下载视频 | 默认不上传，采用许可允许的数据来源说明 |
| .venv、缓存和临时日志 | 不上传 |
| 密码、令牌、私钥、.env 等凭据 | 绝不上传 |

本仓库 `.gitignore` 当前排除了 `.venv/`、`__pycache__/` 和 `videos/` 下除 `self_test_72bpm.mp4` 外的内容。它并不会自动识别所有秘密文件，也不会拦截 `results/` 下的生理数据。

仓库主人此前批准公开现有分析结果，不代表之后每位同学新增的数据自动获得公开许可。未经确认，不要用 `git add -f` 绕过忽略规则。

已跟踪的文件也不会因后来添加 `.gitignore` 而自动停止跟踪。发现误传秘密或隐私数据时立即告知负责人；单纯删除最新版本不能清除历史中的内容。

## 7. 不想用命令行：网页上传少量文件

适合少量文档或小代码文件，不建议用来上传整个实验目录。

1. 接受协作者邀请并进入仓库。
2. 在分支下拉框中从最新 `main` 创建自己的分支，例如 `xiaoming/add-notes`。
3. 确认当前分支不是 `main`。
4. 点击 **Add file → Upload files**，选择需要上传的文件。
5. 检查文件路径，填写提交说明并提交到个人分支。
6. 回到仓库创建该分支到 `main` 的 PR。

网页上传不会依靠你电脑上的 `.gitignore` 替你过滤文件，仍需自己检查视频、凭据与隐私数据。

## 8. 多人改同一个文件：解决冲突

先确认个人分支上的工作已提交，再把最新主分支合并进来：

```bash
git fetch origin
git merge origin/main
```

如果出现冲突：

1. 执行 `git status` 找到冲突文件。
2. 打开文件，理解 `<<<<<<<`、`=======`、`>>>>>>>` 之间的双方修改。
3. 与相关同学讨论，整理成正确的最终代码，删除冲突标记；不要盲目选择全部保留某一边。
4. 运行测试，再暂存已解决的文件并提交。

```bash
git add -- 已解决冲突的文件路径
git commit -m "Resolve conflict with main"
git push
```

若还没想清楚怎么处理，可以用 `git merge --abort` 放弃本次尚未完成的合并。开始合并前保持工作区干净，便于安全恢复。

团队不要使用 `git push --force` 或 `git reset --hard` 处理日常同步问题。

## 9. 未被邀请时：Fork + PR

不需要共享账号，也不需要直接获得原仓库写入权限：

1. 在原仓库点击 **Fork**，复制到自己的 GitHub 账号。
2. 克隆自己的 Fork，而不是原仓库；下面把 `YOUR_USERNAME` 换成你的账号。

```bash
git clone https://github.com/YOUR_USERNAME/rppg.git
cd rppg
git remote add upstream https://github.com/bujuefeng267-byte/rppg.git
git switch -c xiaoming/my-contribution
```

修改后提交并推送到自己的 `origin`：

```bash
git add -- 你实际修改的文件路径
git commit -m "Describe the contribution"
git push -u origin xiaoming/my-contribution
```

在 GitHub 创建跨仓库 PR，目标必须是 `bujuefeng267-byte/rppg` 的 `main`，来源是自己 Fork 的个人分支。

这里 `origin` 是你的 Fork，`upstream` 才是原仓库。同步原仓库主分支时，在干净工作区运行：

```bash
git fetch upstream
git switch main
git merge --ff-only upstream/main
git push origin main
```

参照 [GitHub Fork 协作说明](https://docs.github.com/en/pull-requests/how-tos/work-with-forks)。

## 10. 常见问题

| 问题 | 处理方式 |
|---|---|
| 403 / Permission denied | 确认邀请已接受、当前认证账号是自己、origin 指向正确仓库；否则使用 Fork |
| Repository not found | 检查 URL、账号和访问权限 |
| 网页登录了但终端无法推送 | 当前终端环境还需配置自己的 Git 凭据 |
| non-fast-forward | 先 fetch 并合并同一远程分支，理解冲突后再 push；不要强制覆盖 |
| 视频没有出现在 git status 中 | 查看 .gitignore，这是当前仓库的预期行为 |
| Nothing to commit | 检查是否保存文件、是否在正确仓库、文件是否被忽略 |
| TLS timeout / Connection reset | 属于网络连接问题，不等于密码错误；网络恢复后重试，不关闭证书校验 |
| clone 后缺少真人视频 | 原始视频本来就未上传，按数据来源和许可单独取得 |

本项目此前在 WSL 旧版 Git 上使用节点解析选项未生效，不要照抄聊天记录里的固定 IP 或把临时网络配置当成通用教程。

## 11. 最短协作清单

- 主人：邀请同学；同学：接受邀请并配置自己的 Git 身份与认证。
- 开始：更新 main → 创建个人任务分支。
- 修改：仅添加必要文件，检查隐私、许可、路径和测试结果。
- 上传：commit → push 个人分支 → 创建 PR。
- 审核：检查代码和实验结果 → 合并。
- 下次：更新 main → 新建下一个任务分支。

给同学的一句话：**先在自己的分支上传，再通过 PR 合并；不要直接覆盖 main，不要上传账号凭据或未经允许公开的视频与生理数据。**
