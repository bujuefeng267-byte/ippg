# 当前保留版本与运行入口

目前使用 **V28 连续 rPPG＋V32 独立窗口心率**。它保留已验证的 V32 局部收益；没有一个分支在全部14段录像上都最好。

在 Ubuntu / WSL 仓库根目录建立 `.venv` 并安装 `requirements.txt` 后，对新视频运行：

```bash
./run_retained_hr.sh --video /完整路径/视频.mp4 --out results/new_retained
```

复用已有 V28 输出：

```bash
./run_retained_hr.sh --v28-output results/existing_v28 --out results/new_retained
```

旧批量目录没有本地跟踪缓存时，增加 `--trace-cache /完整路径/frame_trace.csv`，并保留配套 JSON。程序会核验缓存与原输出的绑定。输出目录必须不存在。

| 入口 | 用途 |
|---|---|
| `run_retained_hr.sh` | V28 连续波形＋V32 窗口心率 |
| `run_recommended.sh` / `run_motion_v28.sh` | 保留原 V28 连续模式 |
| `run_motion_v26_experimental.sh` | V26 对照；本次谐波指标对应 `--variant harmonic` |
| `run_pos_experimental.sh` | 固定 POS 对照，从已提取的跟踪缓存运行 |

`window_hr/heart_rate.csv` 是保留方案的心率输出；对应信号在 `window_hr/window_waveforms/`。`continuous_v28/waveform.csv` 是连续波形上下文，不能将其误认为每个 V32 替换心率的来源。缺失心率保持空值。

V26 谐波对照（与发布指标对应）：

```bash
./run_motion_v26_experimental.sh --variant harmonic \
  --video /完整路径/视频.mp4 --out results/new_v26_harmonic
```

POS6 对照运行示例：

```bash
./run_pos_experimental.sh --mode patch6 \
  --trace results/existing_v28/frame_trace.csv --out results/new_pos6
```

需要配套 `frame_trace.json` 和原视频；已有匹配六区域缓存时可增加 `--regional-trace /完整路径/regional_trace.csv`，同时保留其 JSON。缓存绑定原路径、视频身份与校验和，移动后的旧缓存可能需要重新提取；不能改写校验记录强行复用。历史元数据缺少视频SHA时还需原附近的 `inputs_manifest.json`。原始视频与缓存不随本次代码同步。

这些是离线开发集结果，参考同步仍为估计。没有证明临床精度、未知视频泛化能力或接触式 PPG 形态质量。后续实验未通过统一采用检查，因此不进入保留入口。

[完整发布说明与指标](docs/retained_release_20260917.md) · [V28 输入与输出说明](motion_upgrade_v28_20260912/README_video_v28.md) · [V32 实测窗口说明](motion_upgrade_v32_20260914/README.md)
