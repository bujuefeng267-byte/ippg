# 新分支为什么先偏离，再突然下降

当前仍使用原 V28。CDF 是未启用的实验分支；本次核查没有修改推荐算法。

V28 在 data2、data4 已有较好结果。CDF 实验对三个区域的原始与跟踪 RGB 都进行了预处理，然后重跑整个 V28 流程，连用于保底的分支也重新计算。因此，这个实验没有实现“完整保留原 V28 已成功片段”的要求。

## data2：可以直接确认是新分支引入错误

| 图上时间（十秒窗中心） | 参考心率（bpm） | 原 V28 | CDF 实验 |
|---|---:|---:|---:|
| 约 8 秒 | 78.4 | 76 | 162 |
| 约 12 秒 | 80.1 | 76 | 162 |
| 约 13 秒 | 80.2 | 76 | 126 |
| 约 16 秒 | 79.6 | 78 | 126 |
| 约 17 秒 | 79.2 | 79 | 74 |

参考值平稳，原 V28 也接近参考。新分支的 162→126→74 是算法选择的频率改变，不能解释为人的心率突然下降。约 162 bpm 接近参考的二倍，但仅凭这一关系不能证明该峰一定是生理二次谐波。

核对程序记录：

- data2 整段 `route_eligible` 均为 false，最终波形的 `candidate_weight` 均为 0，最终波形与 **CDF 后重算的 fallback** 相同。问题已经出现在被 CDF 改变的保底信号中，不是原 V28 输出被保持住了。
- 约 16→17 秒，选中的实际峰从 125 变到 75 bpm，输出网格状态从 126 变到 74；`evidence_reacquired=true`。
- 后一窗最强功率峰仍约 124 bpm，但 75 bpm 候选结合周期、谐波和运动证据后的评分更高，所以程序换到另一条频率轨迹。
- 当前心率跟踪允许付出固定代价后重新捕获另一候选频率，12 bpm/s 只约束普通转移，不是所有输出之间的硬限制。这里使用离线动态规划，不能描述为实时“等待到第 17 秒才收敛”。

## data6：也有跳频，且受到相邻窗口融合影响

约 17→18 秒，CDF 输出从 190 变到 97 bpm，参考为 110.5→109.6 bpm。新窗口的 97 bpm 候选刚达到约 5.44% 的相对功率门槛，并因周期与运动评分获得比高频候选更高的分数，触发重新捕获。下降后仍未进入参考 ±5 bpm。

不能仅凭该窗口 `route_eligible=false` 判断没有使用候选波形：重叠加权会把相邻窗口的替换贡献带进来。14–18 秒中心的分析窗内，最大候选混合权重约为 0.087、0.198、0.329、0.448、0.510。本例与 data2 全程纯 CDF fallback 的情况不同。

## 后续修改要求

1. 保持原始 V28 的 RGB、波形与心率计算路径完整，新方法作为独立候选分支；保护对象必须是真正的原 V28 输出。
2. 只有新分支具有可从视频独立计算的可靠证据时才考虑局部介入；测试参考值只用于评价，不进入切换规则。
3. 检查替换边界与重叠窗口，避免局部改动扩散到原本应保留的片段。
4. 将 V28 原来正确的窗口单独列为回归检查，同时报告原来错误窗口的改善、开头大误差和跳频情况，不能只凭总体 MAE 下降决定采用。

重点是避免一开始选错频率，仅增加曲线平滑不能修复错误的早期估计。这些是后续实现要求，当前没有宣称已实现一个能自动识别全部错误片段的保护器。

## 核查来源

- [data2 配对输出](<//wsl.localhost/Ubuntu/home/fengbujue/项目/rppg识别/results/data1_6_v30_20260912/cdf_v28/data2/evaluation/paired_windows.csv>)
- [data2 候选与跟踪记录](<//wsl.localhost/Ubuntu/home/fengbujue/项目/rppg识别/results/data1_6_v30_20260912/cdf_v28/data2/heart_rate.csv>)
- [data2 分支选择记录](<//wsl.localhost/Ubuntu/home/fengbujue/项目/rppg识别/results/data1_6_v30_20260912/cdf_v28/data2/routing_decisions.csv>)
- [data6 候选与跟踪记录](<//wsl.localhost/Ubuntu/home/fengbujue/项目/rppg识别/results/data1_6_v30_20260912/cdf_v28/data6/heart_rate.csv>)
- [原 V28 心率候选评分与离线动态规划](<//wsl.localhost/Ubuntu/home/fengbujue/项目/rppg识别/motion_upgrade_v28_20260912/evidence_hr.py>)

时间沿用原估计同步，参考是窗口内平均心率；这里只做已保存输出的错误诊断，没有重新调参或替换任何历史输出。
